// function lists tasks with deadline in ASC/DESC

// function submit form for edit lists
const submitList = document.querySelectorAll($list['id']);
document.querySelector('.submit'),
  addEventListener('click', function () {
    submitList.submit();
  });
